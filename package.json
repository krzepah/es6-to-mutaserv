{
  "name": "mutaserv-formater",
  "version": "0.0.0",
  "description": "Format ES6 mutations to one specificly used by mutaserv",
  "scripts": {
    "test": "exit 0",
    "release": "npm t && git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags && npm publish"
  },
  "author": "krzepah <patrick@krzepah.net>",
  "license": "MIT",
  "bin": {
    "mutaserv-format": "./index.js"
  },
  "eslintConfig": {
    "extends": "eslint-config-developit",
    "rules": {
      "prefer-rest-params": 0
    }
  },
  "devDependencies": {
    "eslint": "^6.1.0",
    "eslint-config-developit": "^1.1.1"
  },
  "dependencies": {
    "@babel/parser": "^7.5.5",
    "@babel/generator": "^7.5.5",
    "ramda": "^0.26.1"
  }
}
